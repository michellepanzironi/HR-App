import { render, screen } from '@testing-library/react';
import Person from '../App';

test('Allow the user to reject or approve a candidate', () => {
  // render(<Person />);
  // const button = screen.getByRole('button')
  // console.log(button)

  // expect(button).toHaveLength(1);
});

